
function main() {
    if (document.getElementById(""))
        return
    const canvas = document.querySelector("#earth_shad");
    const gl = canvas.getContext("webgl");
    if (gl === null) {
        alert("Unavle to view earth due to browser functionality");
        return;
    }
}